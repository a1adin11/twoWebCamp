export interface IRatingProps {
  squad: number;
  name?: string;
  score: number;
  img: string;
}

export interface IOrgProps {
  name: string;
  text: string;
  img: string;
}

export interface IVideoData {
  prev: string;
  src: string;
}
